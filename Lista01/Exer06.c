/* Esta faltando #include <stdio.h> e <stdlib.h>. 
Esta faltando o int antes do main. No final da 
declaração das variáveis tem (:) no fim da linha e 
o certo é usar (;), além das variáveis estarem sendo
separas por (;) e o certo é usar (,). Na função 
prinf ficou faltando fechar as "" e tem uma variável
a mais (d) que não foi declarada antes. Caso o 
intuito seja que os números fiquem separados, tem 
que dar espaços nos %d. E por fim ficou o return 0
para finalizar o programa. */

